SET NOCOUNT ON; select * from C_REL_DEALER_BRAND where exists(select 1 from C_B_DEALER where HUB_STATE_IND=1and C_REL_DEALER_BRAND.DEALER_ID=C_B_DEALER.ROWID_OBJECT ) and HUB_STATE_IND=1 ORDER BY CAST(ROWID_OBJECT AS INT)